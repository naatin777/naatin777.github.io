---
import { socialLink, socialIcon } from "../../styles/social-link.css";
import GitHub from "../../assets/sns/github.svg";

interface Props {
    url: string;
    label: string;
}

const { url, label } = Astro.props;
---

<a
    href={url}
    target="_blank"
    rel="noopener noreferrer"
    class={socialLink}
    aria-label={label}
>
    <img src={GitHub.src} alt="GitHub" class={socialIcon} />
</a>
